export default function AdminEventsPage() {
  return (
    <div className="surface-elevated p-8 rounded-2xl border border-[var(--border)]">
      <h2 className="text-2xl font-bold mb-4" style={{ color: 'var(--text-primary)' }}>Manage Events</h2>
      <p className="text-[var(--text-muted)]">Data table for events (Create, Read, Update, Delete) will be initialized here.</p>
    </div>
  );
}
