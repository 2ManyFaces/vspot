export default function AdminUsersPage() {
  return (
    <div className="surface-elevated p-8 rounded-2xl border border-[var(--border)]">
      <h2 className="text-2xl font-bold mb-4" style={{ color: 'var(--text-primary)' }}>Manage Users</h2>
      <p className="text-[var(--text-muted)]">Data table for user statistics (Ban, View, Update) will be initialized here.</p>
    </div>
  );
}
